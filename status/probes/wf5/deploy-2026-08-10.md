# Slack channel boundaries — live deployment record

> **Note added 2026-08-10 (later the same day) — the record below is
> accurate; half of what it deployed is withdrawn.** The mention-routing
> half stands and stays live. The **toolset-gating half** (VM commit
> `5af9e1e`, the `platform_tool_conversations` config key) implemented a
> misread instruction — Gaetan asked for tool-call *display* to be scoped to
> the home channel and his DM, not tool *capability* — and a later fresh
> mention in `GQ07CQXT7` landed in a toolless session that could not
> delegate. Nothing below is rewritten: this is what was deployed, and it is
> still what the VM is running. Its removal, plus the corrected display-only
> patch, is planned in `docs/plans/apply-slack-channel-boundaries.md`;
> evidence in `status/probes/wf5/slack-tool-visibility-correction.md`.
>
> Two later context corrections (2026-08-10 evening, restore dispatch):
> (1) the "39-test handler suite" cited below was withdrawn with the gate —
> the surviving 10 mention tests live in
> `tests/gateway/test_slack_tool_visibility.py`, and
> `test_group_dm_unmentioned_is_ignored` shipped as
> `test_group_dm_unmentioned_thread_reply_is_ignored`. (2) An evening
> re-measurement found `display.platforms.slack.tool_progress: all` live,
> although the 2026-08-08 record set `off` and this record's config diff
> lists no `display` edits — unrecorded drift, reconciled in
> `status/probes/wf6/slack-tool-visibility-restore.md`.

2026-08-10 ~14:36–14:45 UTC, Orca worker session, coordinator-approved gate
(reply: "go — deploy the corrected commit exactly as briefed"). Deployed the
corrected patch (`302ed2e` lineage) per
`docs/plans/apply-slack-channel-boundaries.md`.

## What was applied

- **Code**: `patches/0002-per-conversation-toolset-gating.patch` via
  `git am --3way` onto `~/.hermes/hermes-agent` (tip was `d615ca8`, clean but
  for unrelated JS lockfile drift, left untouched). Landed as VM commit
  `5af9e1e` — hash differs from `302ed2e` only by committer metadata
  (identity supplied inline with `-c`; none configured on the VM);
  **sha256 of all three changed files verified identical** to the corrected
  scratch tree.
- **Config**: `~/.hermes/config.yaml`, backup
  `config.yaml.bak-boundaries-20260810-143638`, edited under
  `flock ~/.hermes/.wf3.lock` with a ruamel round-trip script
  (`hermes config set` was rejected: it rewrites the file with plain PyYAML
  and drops comments). Diff verified — only the intended changes:
  - top-level `slack:` — deleted `allowed_channels: C0BP2GZUFSR,C0BFQ5WFYTB`;
    now `require_mention: true`, `strict_mention: true`,
    `free_response_channels: C0BP2GZUFSR`, `unauthorized_dm_behavior: ignore`.
  - `gateway.platforms.slack` — removed the three shadowed duplicates
    (`require_mention`, `strict_mention`, `unauthorized_dm_behavior`);
    `enabled: true` kept.
  - new top-level `platform_tool_conversations: {slack: [C0BP2GZUFSR,
    D0BBYNM01BL]}`.
  - ruamel reflowed some list indentation/string wrapping (cosmetic; YAML
    parse verified).
- **Restart**: `systemctl --user restart hermes-gateway.service` 14:37:08 UTC.
  Slack socket authenticated + connected at 14:37:11. The interrupted
  in-flight state drained with a warning (expected; auto-resume path).

## Verification — all PASS

Effective adapter values (throwaway-process loader check, run pre- and
post-restart, identical): `strict True | require True | free ['C0BP2GZUFSR']
| allowed []` (empty = no channel whitelist), `reaction_triggers None`,
`mention_patterns []`.

Tool-gate assert on live code + live config:
`{'C0BP2GZUFSR': 21, 'D0BBYNM01BL': 21, 'C0BFQ5WFYTB': 0, 'G_SYNTH_MPIM': 0,
'': 0}` — asserted, not eyeballed.

Live routing probes (sent as Gaetan via the VM-stored native creds, secrets
never on argv; replies polled via `conversations.replies`):

| # | Probe | Expected | Observed |
|---|---|---|---|
| P1 | `C0BFQ5WFYTB` top-level, unmentioned | silence | 0 replies; **no** `inbound message` log line — dropped in-handler |
| P2 | `C0BFQ5WFYTB` fresh `@tars` | reply, zero tools | in-thread "Deploy check passed." — session `20260810_144014_9507398a`, `api_calls=1`, `text_response`, no `tool_executor` lines |
| P3 | unmentioned "merci !" in P2's thread, session active (the original regression, re-armed live) | silence | 0 replies; not processed |
| P4 | `C0BP2GZUFSR` top-level, unmentioned | reply, tools | replied after a `kanban_show` progress bubble — free-response + tools retained |
| P5 | `D0BBYNM01BL` 1:1 DM, unmentioned | reply, tools | replied with `kanban_show` bubble — mention-exempt + tools retained |

MPIM: not probed live (creating an MPIM would ping a third party); covered
by the synthetic tool-gate assert (`G_SYNTH_MPIM: 0`) and the 39-test
handler suite (`test_group_dm_fresh_mention_is_processed`,
`test_group_dm_unmentioned_is_ignored`).

Post-probe health: unit `active`, `errors.log` shows only routine
`kanban_show returned error` noise from the two tool-entitled probe sessions
(home 14:39, DM 14:42) — none from the boundary-channel session. VM `/tmp`
probe helpers and patch copy removed.

## Rollback (unused)

`git -C ~/.hermes/hermes-agent reset --hard d615ca8`, restore
`config.yaml.bak-boundaries-20260810-143638` under the same flock, restart.
