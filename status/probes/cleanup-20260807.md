# Cleanup — 2026-08-07 (GITHUB_PAT row + Notion export-path cancellation)

Per Gaetan's triage (`status/wf4-report.md` §"GAPs → new probe stubs", 2026-08-07 evening):
§21 `GITHUB_PAT` row reconcile, and Notion export path CANCELLED. VM `gaetan@192.168.0.9`. No
`git` run (repo is a plain checkout here — commit is Gaetan's own step). No units restarted,
`config.yaml` and `192.168.0.3` never touched.

## 1 — `GITHUB_PAT` row (`docs/specs/tars-profile.md` §3)

Probe 21 (`docs/specs/wf4-probes.md` §21) found `GITHUB_PAT` listed in the credential map but
present in neither `~/.hermes/.env` nor `secrets/tars.sops.yaml` — the exercised path is `gh`
device-flow on the VM (`status/lane-a.md` A1: "GitHub — `gh auth login` device-flow ON the VM
(Gaetan's call; replaces the PAT)", SOPS key `—`). Applied probe 21's Action option 1: deleted
the stale row, replaced with the device-flow reality.

Before:
```
| `GITHUB_PAT` | SOPS `GITHUB_PAT` | A1 |
```

After:
```
| `GITHUB_PAT` | **not set — `gh auth login` device-flow on the VM, self-managed, never in SOPS** (Gaetan's call; see `status/lane-a.md` A1) | A1 |
```

Scope: only `docs/specs/tars-profile.md` §3, as instructed — `docs/recon/DECISION.md` D5's own
`GITHUB_PAT` row was left untouched (not in scope).

## 2 — Notion export path cancellation (Gaetan: Notion is MCP live-fetching only)

### 2a — VM `~/.hermes/.env`

Backup, rebuild, atomic swap, all under `flock ~/.hermes/.wf3.lock`:
```
flock ~/.hermes/.wf3.lock -c "
  cp ~/.hermes/.env ~/.hermes/.env.bak-notiontrim && chmod 600 ~/.hermes/.env.bak-notiontrim
  umask 077
  grep -vE '^(NOTION_TOKEN_V2|NOTION_FILE_TOKEN|NOTION_SPACE_ID)=' ~/.hermes/.env > ~/.hermes/.env.new
  mv ~/.hermes/.env.new ~/.hermes/.env
  chmod 600 ~/.hermes/.env
"
```

Key names only (`cut -d= -f1`), never values:

| | Before (14 keys) | After (11 keys) |
|---|---|---|
| | GMAIL_ADDRESS, GMAIL_APP_PASSWORD, HINDSIGHT_MODE, LINEAR_API_KEY, **NOTION_API_TOKEN**, ~~NOTION_FILE_TOKEN~~, ~~NOTION_SPACE_ID~~, ~~NOTION_TOKEN_V2~~, SLACK_ALLOWED_USERS, SLACK_APP_TOKEN, SLACK_BOT_TOKEN, SLACK_HOME_CHANNEL, SLACK_MCP_XOXC_TOKEN, SLACK_MCP_XOXD_TOKEN | GMAIL_ADDRESS, GMAIL_APP_PASSWORD, HINDSIGHT_MODE, LINEAR_API_KEY, **NOTION_API_TOKEN**, SLACK_ALLOWED_USERS, SLACK_APP_TOKEN, SLACK_BOT_TOKEN, SLACK_HOME_CHANNEL, SLACK_MCP_XOXC_TOKEN, SLACK_MCP_XOXD_TOKEN |

Verified:
- Count: 14 − 3 = 11. ✓
- `NOTION_API_TOKEN` (the MCP server token) still present. ✓
- Mode: `~/.hermes/.env` = 600, `~/.hermes/.env.bak-notiontrim` = 600 (`stat -c "%n %a"`). ✓
- No leftover `~/.hermes/.env.new` (atomic `mv` completed). ✓
- Gateway **not** restarted: `hermes-gateway.service` `active (running) since Fri 2026-08-07
  19:40:45 UTC` — pre-dates this change (made ~20:13 UTC). Removal takes effect at the unit's
  next natural restart, not before.

### 2b — `secrets/tars.sops.yaml` (cooper)

`sops --version` = 3.10.2; `sops unset --help` confirms the subcommand is supported
(`sops unset [command options] file index`). Ran per-key, never a whole-file `sops -d`:
```
sops unset secrets/tars.sops.yaml '["NOTION_TOKEN_V2"]'
sops unset secrets/tars.sops.yaml '["NOTION_FILE_TOKEN"]'
sops unset secrets/tars.sops.yaml '["NOTION_SPACE_ID"]'
```
All three exited clean (no error output).

Key names only (`grep -oE '^[A-Z_0-9]+:'` — matches keys, never decrypts a value):

| Before (11 keys) | After (8 keys) |
|---|---|
| ~~NOTION_TOKEN_V2~~, ~~NOTION_FILE_TOKEN~~, ~~NOTION_SPACE_ID~~, LINEAR_API_KEY, GMAIL_ADDRESS, GMAIL_APP_PASSWORD, **NOTION_API_TOKEN**, SLACK_TOKEN, SLACK_COOKIE, SLACK_BOT_TOKEN, SLACK_APP_TOKEN | LINEAR_API_KEY, GMAIL_ADDRESS, GMAIL_APP_PASSWORD, **NOTION_API_TOKEN**, SLACK_TOKEN, SLACK_COOKIE, SLACK_BOT_TOKEN, SLACK_APP_TOKEN |

Verified:
- `grep -E "^(NOTION_TOKEN_V2|NOTION_FILE_TOKEN|NOTION_SPACE_ID):" secrets/tars.sops.yaml` →
  no match (exit 1). ✓
- `grep -c "^NOTION_API_TOKEN:" secrets/tars.sops.yaml` → 1 (still present). ✓
- File remains SOPS-encrypted (`sops:` / `age:` / `mac:` metadata intact, values still
  `ENC[AES256_GCM,...]`) — no whole-file decrypt was ever run.

### 2c — `docs/specs/tars-profile.md` §3 `.env`↔SOPS map

- Dropped the `NOTION_TOKEN_V2` / `NOTION_FILE_TOKEN` / `NOTION_SPACE_ID` table rows.
- Added a note under "Not in `.env`" marking the export path **CANCELLED 2026-08-07 (Gaetan)**:
  Notion is MCP live-fetching only (`NOTION_API_TOKEN`, lane A2), pointing at this evidence file.
- Added a note distinguishing `NOTION_API_KEY` (the bundled hermes `notion` SKILL's own
  credential — separate from the MCP server's `NOTION_API_TOKEN`) as dropped/never-provisioned,
  since the SKILL is not in use.

## What was NOT touched (per instructions)

Whole-file `sops -d` never run · no secret value echoed (all verification was key-name-only via
`cut -d=` / `grep -oE`) · no systemd unit restarted · `~/.hermes/config.yaml` untouched ·
`192.168.0.3` never contacted · no `git` commands run (commit left to Gaetan).

## Verdict

**PASS.** Both cleanup items landed with the requested evidence: GITHUB_PAT row now states the
device-flow reality; Notion export trio removed from both the VM `.env` (14→11 keys,
`NOTION_API_TOKEN` retained, mode 600, gateway untouched) and `secrets/tars.sops.yaml`
(`sops unset`, 11→8 keys, `NOTION_API_TOKEN` retained, still encrypted); docs updated to match.
